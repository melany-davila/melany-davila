import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://melany-davila.github.io',
  base: '/melany-davila' // Si tu repositorio se llama melany-davila. (Si fuera melany-davila.github.io, puedes omitir 'base')
});

